<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms" 
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:Chat.Views"
       Title="Chat"
       x:Class="Chat.AppShell">

    <ShellContent Route="login"
                  ContentTemplate="{DataTemplate local:LoginPage}">
    </ShellContent>

    <ShellContent Route="signup"
                  ContentTemplate="{DataTemplate local:NewUserPage}">
    </ShellContent >

    <TabBar>
     <ShellContent Route="home" Title="Chat"
                       IsTabStop="True"
                    ContentTemplate="{DataTemplate local:ChatListPage}"
                   />
        </TabBar>

</Shell>
